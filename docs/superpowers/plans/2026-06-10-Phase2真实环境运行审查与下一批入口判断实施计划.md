# Phase 2 真实环境运行审查与下一批入口判断 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 Phase 2 ManiSkill/SAPIEN 真实环境 500 requested samples 运行结果沉淀为可审查证据，并同步项目入口文档与下一阶段路线判断。

**Architecture:** 不修改 accumulation pipeline 核心逻辑；把本轮运行事实写入 `docs/evidence/simulation-runs/`，同步 `README.md` 与 `details.md`，再刷新 HTML 阅读版。为保持工作区可用，补充 ignore 规则，避免本地仿真 artifacts 和 editable install 产物进入 git 状态。

**Tech Stack:** Markdown、简单 HTML 阅读版、现有 ManiSkill/SAPIEN accumulation CLI、pytest、git/GitHub CLI。

---

## Spec Reference

- Spec: `docs/superpowers/specs/2026-06-10-Phase2真实环境运行审查与下一批入口判断-design.md`
- 已运行命令：

```bash
conda run -n weld-maniskill python -m weldcore.simulation_bakeoff.maniskill_accumulation_pipeline \
  --outdir artifacts/simulation/maniskill-sapien-accumulations \
  --accumulation-id maniskill-sapien-accumulation-phase-2 \
  --shards 5 \
  --samples-per-task 50
```

- 首次运行：500 requested / 500 completed / 0 failed / 0 skipped；5 个 shard 均为 `completed_new_run`。
- 第二次复用：500 requested / 500 completed / 0 failed / 0 skipped；5 个 shard 均为 `reused_existing_result`。
- `failure_boundary_counts` 为空。
- completed 样本关键字段覆盖率均为 1.0：`raw_artifact_uri`、`adapter_result_uri`、`experience_dataset_uri`、`evidence_bundle_uri`。
- 当前状态应写作 `ready_to_scale_with_conditions`；不要写成最终仿真器选型、真实焊接质量验证或真实机器人执行验证。

## File Structure

Create:

- `docs/evidence/simulation-runs/2026-06-10-phase2-maniskill-sapien-review.md`
  - 记录运行环境、命令、首次运行分布、复用分布、failure boundary、字段覆盖、判断和下一步。

Modify:

- `.gitignore`
  - 增加 `weld-experience-engine/artifacts/simulation/` 与 `weld-experience-engine/weldcore.egg-info/`，保持本地运行产物不污染工作区。
- `README.md`
  - 从“下一步运行 Phase 2”更新为“Phase 2 已完成真实环境运行审查，下一步进入 1000 requested samples 计划准备”。
  - 保留 Phase 1/Phase 2 命令和边界。
- `details.md`
  - 在 2026-06-10 更新块中记录 Phase 2 真实环境运行与复用结果。
  - 更新“下一步建议”为下一批默认入口后的 1000 requested samples 计划。
- `README.html`
- `details.html`
  - 从 Markdown 维护源刷新，不手写独立内容。

Do not modify:

- `weld-experience-engine/weldcore/simulation_bakeoff/*.py`
- `weld-experience-engine/tests/*.py`
- 任何真实机器人、Gazebo/MoveIt、Isaac、ROS 相关模块。

---

### Task 1: Evidence Run Review Document

**Files:**
- Create: `docs/evidence/simulation-runs/2026-06-10-phase2-maniskill-sapien-review.md`
- Modify: `.gitignore`

- [ ] **Step 1: Write the review document**

Create `docs/evidence/simulation-runs/2026-06-10-phase2-maniskill-sapien-review.md` with this structure:

```markdown
# Phase 2 ManiSkill/SAPIEN 真实环境运行审查

日期：2026-06-10

## 结论

Phase 2 在 `weld-maniskill` conda 环境完成 5 shards x 100 requested samples，共 500 requested samples 的真实 ManiSkill/SAPIEN headless backend probe。首次运行 500/500 completed，第二次同命令复跑 5 个 shard 均复用已有 `batch_result.json`。

当前判断是 `ready_to_scale_with_conditions`：ManiSkill/SAPIEN 可以作为下一批 accumulation 默认入口继续使用，但这不是最终仿真器选型、真实焊接质量验证或真实机器人执行验证。

## 运行环境

- conda env：`weld-maniskill`
- `mani_skill`：可导入
- `sapien`：可导入
- `weldcore`：当前 worktree editable install
- 默认 `uv` 环境：保持轻量，不内置 ManiSkill/SAPIEN

## 运行命令

```bash
conda run -n weld-maniskill python -m weldcore.simulation_bakeoff.maniskill_accumulation_pipeline \
  --outdir artifacts/simulation/maniskill-sapien-accumulations \
  --accumulation-id maniskill-sapien-accumulation-phase-2 \
  --shards 5 \
  --samples-per-task 50
```

## 首次运行分布

| 指标 | 结果 |
| --- | --- |
| requested samples | 500 |
| completed samples | 500 |
| failed samples | 0 |
| skipped samples | 0 |
| shard status | 5 x `completed_new_run` |
| report status | `ready_to_scale_with_conditions` |

## 复用运行分布

| 指标 | 结果 |
| --- | --- |
| requested samples | 500 |
| completed samples | 500 |
| failed samples | 0 |
| skipped samples | 0 |
| shard status | 5 x `reused_existing_result` |
| report status | `ready_to_scale_with_conditions` |

## Failure Boundary Counts

`failure_boundary_counts` 为空。本轮没有出现环境缺失、仿真运行失败、adapter 转换失败、dataset 导出失败或 data contract 不完整。

## Field Coverage

5 个 shard 的 completed sample coverage 均为：

| 字段 | 覆盖率 |
| --- | --- |
| `raw_artifact_uri` | 1.0 |
| `adapter_result_uri` | 1.0 |
| `experience_dataset_uri` | 1.0 |
| `evidence_bundle_uri` | 1.0 |
| `failure_artifact_uri` | 0.0 |

`failure_artifact_uri` 为 0.0 是预期结果，因为本轮没有 failed samples。

## Warning 与边界

运行中出现 SAPIEN Vulkan fallback 与 pinocchio 缺失 warning，但没有导致样本失败，也没有进入 failure boundary。

本轮仍只证明 ManiSkill/SAPIEN headless backend probe 和项目数据契约可在 500 requested samples 级别稳定运行；不证明真实焊接质量，不构成 WPS/PQR，不表示真实机器人可执行，也不表示最终仿真器选型完成。

## 下一步

建议下一阶段进入 1000 requested samples 计划准备：保持当前 2 个默认任务族，继续使用 ManiSkill/SAPIEN 作为默认 accumulation 入口；在扩大前先修正 report 中仍偏 Phase 1 的 `next_scale_recommendation` 文案。
```

- [ ] **Step 2: Update `.gitignore` for local artifacts**

Append if absent:

```gitignore
weld-experience-engine/artifacts/simulation/
weld-experience-engine/weldcore.egg-info/
```

Do not ignore tracked docs or source files.

- [ ] **Step 3: Verify review document and ignore rules**

Run:

```bash
rg -n "500|completed_new_run|reused_existing_result|ready_to_scale_with_conditions|不证明真实焊接质量|最终仿真器" docs/evidence/simulation-runs/2026-06-10-phase2-maniskill-sapien-review.md
git status --short
```

Expected:

- The review document includes the run distribution, reuse distribution and boundary language.
- `weld-experience-engine/artifacts/` and `weld-experience-engine/weldcore.egg-info/` no longer appear as untracked entries.
- `weld-experience-engine/uv.lock` may still appear if generated; only remove it after confirming it is untracked and generated by this run.

- [ ] **Step 4: Commit Task 1**

```bash
git add .gitignore docs/evidence/simulation-runs/2026-06-10-phase2-maniskill-sapien-review.md
git commit -m "docs: record phase2 maniskill run review"
```

---

### Task 2: README And Details Update

**Files:**
- Modify: `README.md`
- Modify: `details.md`

- [ ] **Step 1: Update `README.md`**

Change the current positioning and next-stage text so it says:

- Phase 2 has now been run in the real `weld-maniskill` environment.
- The run produced 500 requested / 500 completed / 0 failed / 0 skipped.
- A second same-command run reused all 5 shard `batch_result.json` files.
- `failure_boundary_counts` is empty.
- Completed sample field coverage is stable at 1.0 for raw artifact, adapter result, experience dataset and evidence bundle.
- The next step is not “run Phase 2” anymore; it is preparing the next default batch, likely 1000 requested samples, while first fixing `next_scale_recommendation` wording if needed.
- Keep boundary language: not final simulator selection, not real welding quality, not robot execution validation.

Do not remove Phase 1 or Phase 2 commands.

- [ ] **Step 2: Update `details.md`**

In `### 2026-06-10`, add bullets for:

- Phase 2 real environment run completed in `weld-maniskill`.
- First run: 500 requested / 500 completed / 0 failed / 0 skipped; 5 x `completed_new_run`.
- Reuse run: 5 x `reused_existing_result`.
- `failure_boundary_counts` empty.
- Completed sample key field coverage stable at 1.0.
- Status remains `ready_to_scale_with_conditions`; `locked_for_next_batch_with_conditions` was not needed because no failed samples appeared.

Update “下一步建议” to:

- Prepare 1000 requested samples next-batch plan.
- Keep current 2 default task families.
- Continue ManiSkill/SAPIEN as default accumulation entry under conditions.
- First fix report recommendation wording if it still says to continue Phase 1 before Phase 2.
- If next batch fails, fix failure boundaries before switching simulator or moving to real robots.

- [ ] **Step 3: Verify Markdown content**

Run:

```bash
rg -n "500 requested|500 completed|reused_existing_result|ready_to_scale_with_conditions|1000 requested|最终仿真器|真实焊接质量|真实机器人" README.md details.md
```

Expected:

- Both Markdown files mention the Phase 2 run result.
- Both preserve boundary language.
- No text says Phase 2 still has not been run.

- [ ] **Step 4: Commit Task 2**

```bash
git add README.md details.md
git commit -m "docs: update phase2 run status"
```

---

### Task 3: HTML Refresh

**Files:**
- Modify: `README.html`
- Modify: `details.html`

- [ ] **Step 1: Refresh HTML from Markdown**

If `pandoc` is available, use it only if it preserves the existing simple readable style. If not, use a local one-off Python conversion command that is executed from the shell and not saved into the repository. The one-off command should:

- Parses headings, paragraphs, unordered lists, ordered lists, code fences and inline code enough for these two files.
- Reuses the existing HTML style block.
- Sets the title to the Markdown H1.
- Sets the meta source link to `README.md` or `details.md`.

Do not hand-edit HTML content independently from Markdown. Do not add a reusable converter script unless a later task explicitly asks for one.

- [ ] **Step 2: Verify HTML content**

Run:

```bash
rg -n "500 requested|500 completed|reused_existing_result|ready_to_scale_with_conditions|1000 requested|最终仿真器|真实焊接质量|真实机器人" README.html details.html
```

Expected:

- HTML files contain the same new facts and boundary language as Markdown.

- [ ] **Step 3: Commit Task 3**

```bash
git add README.html details.html
git commit -m "docs: refresh phase2 html reading copies"
```

---

### Task 4: Verification And Delivery Prep

**Files:**
- Read: `README.md`
- Read: `details.md`
- Read: `docs/evidence/simulation-runs/2026-06-10-phase2-maniskill-sapien-review.md`
- Read: `weld-experience-engine/tests/`

- [ ] **Step 1: Clean generated local files carefully**

Run:

```bash
git status --short -- weld-experience-engine/uv.lock
git ls-files -- weld-experience-engine/uv.lock
test -f weld-experience-engine/uv.lock && git status --short -- weld-experience-engine/uv.lock
```

If and only if `weld-experience-engine/uv.lock` is untracked and was generated by this run, remove it:

```bash
rm weld-experience-engine/uv.lock
git status --short
```

Expected:

- No untracked `uv.lock`.
- Ignored local simulation artifacts do not appear.
- Only intended tracked modifications remain, if any.

- [ ] **Step 2: Run focused verification**

Run:

```bash
cd weld-experience-engine
uv run pytest tests/test_maniskill_accumulation_pipeline.py tests/test_simulation_accumulation_models.py -q
```

Expected: PASS.

- [ ] **Step 3: Run full verification**

Run:

```bash
cd weld-experience-engine
uv run pytest -q
```

Expected: PASS.

- [ ] **Step 4: Inspect final diff**

Run:

```bash
git diff --stat origin/main...HEAD
git log --oneline origin/main..HEAD
git status --short
```

Expected:

- Commits include spec, review docs, README/details and HTML refresh.
- No generated artifacts are staged or untracked.

- [ ] **Step 5: Commit any remaining verification cleanup**

If Step 4 shows intentional tracked changes not yet committed, commit them with a docs-focused message. Do not commit artifacts.

---

### Task 5: PR, Remote Merge, And Local Branch Cleanup

**Files:**
- Read: all committed changes

- [ ] **Step 1: Push branch**

Run:

```bash
git status --short
git push -u origin codex/phase2-real-run-review
```

Expected:

- Working tree is clean before push.
- Branch is available on origin.

- [ ] **Step 2: Create PR**

Run:

```bash
gh pr create \
  --base main \
  --head codex/phase2-real-run-review \
  --title "docs: record Phase 2 ManiSkill run review" \
  --body "## Summary
- record Phase 2 ManiSkill/SAPIEN 500 requested samples run review
- update README/details and HTML reading copies with the run result
- keep generated simulation artifacts out of git status

## Verification
- cd weld-experience-engine && uv run pytest tests/test_maniskill_accumulation_pipeline.py tests/test_simulation_accumulation_models.py -q
- cd weld-experience-engine && uv run pytest -q"
```

Expected:

- PR URL is returned.

- [ ] **Step 3: Merge PR remotely**

After the PR is created and checks are acceptable, run:

```bash
gh pr merge --merge --delete-branch
```

Expected:

- PR is merged into `main`.
- Remote branch is deleted by GitHub.

- [ ] **Step 4: Update local main**

Run from the original project root, not the worktree:

```bash
git switch main
git pull --ff-only origin main
```

Expected:

- Local `main` contains the merged PR.

- [ ] **Step 5: Remove local worktree and branch**

Run from the original project root:

```bash
git worktree remove "$HOME/.config/superpowers/worktrees/A02-焊接技能大师平台/phase2-real-run-review"
git branch -d codex/phase2-real-run-review
git worktree prune
```

Expected:

- Worktree directory is removed.
- Local feature branch is deleted.
- `git worktree list` no longer shows the Phase 2 review worktree.
